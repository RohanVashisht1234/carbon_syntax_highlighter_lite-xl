package ExplorerTest api;

// A syntax highlighter for carbon programming language in lite-xl!
fn Main() -> i32 {
    let name: auto = "Hello! from Rohan Vashisht!";
    let age: i32 = 18;

    Print(name);
    Print("My age is: {0}", age);
    return 0;
}
