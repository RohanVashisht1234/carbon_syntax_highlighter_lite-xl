package ExplorerTest api;

// A syntax highlighter for carbon programming language in lite-xl!
fn Main() -> i32 {
    var name: auto = "Hello! from Rohan Vashisht!";
    Print(name);
    var age: i32 = 18;
    Print(age);
    return 0;
}

